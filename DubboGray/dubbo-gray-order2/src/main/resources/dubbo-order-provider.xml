<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
	http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    
    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="dubbo-gray-order-provider"  />
    
    <!-- 注册中心暴露服务地址  -->
 	<dubbo:registry address="zookeeper://${zookeeper.url}" />
 	
    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20899" />
    
    <!-- 和本地bean一样实现服务 -->
	<bean id="orderService" class="com.candao.dubbo.gray.order.service.OrderService" />
	<dubbo:service interface="com.candao.dubbo.gray.order.api.IOrderService" ref="orderService" loadbalance="grayrandom"/>
	
	<dubbo:provider filter="-exception"/>
</beans>